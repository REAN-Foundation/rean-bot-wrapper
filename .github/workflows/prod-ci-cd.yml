---
name: PROD-CI-CD
on:
  push:
    branches:
      - main1
jobs:
  - name: New Release Publish
    uses: release-drafter/release-drafter@v5
    with:
      publish: true
    env:
      GITHUB_TOKEN: ${{ secrets.PAT_GITHUB }}
